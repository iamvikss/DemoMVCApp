@model DemoMVCApp.MyApplication

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>MyApplication</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.MyApplicationID)

        <div class="form-group">
            @Html.LabelFor(model => model.MyApplicationName, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.MyApplicationName)
                @Html.ValidationMessageFor(model => model.MyApplicationName)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.LastModifiedDateTime, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.LastModifiedDateTime)
                @Html.ValidationMessageFor(model => model.LastModifiedDateTime)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
